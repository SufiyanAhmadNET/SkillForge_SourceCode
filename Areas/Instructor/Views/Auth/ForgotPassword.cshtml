@{
    Layout = "~/Areas/Pubic/Views/Shared/_AuthLayout.cshtml";
    ViewData["Title"] = "Instructor Recovery";
    ViewData["RoleClass"] = "role-instructor";
}

@* ================= Recovery Section ================= *@

<section class="d-flex align-items-center justify-content-center py-5 bg-light flex-grow-1">
    <div class="card auth-card shadow-sm">
        
        <div class="text-center mb-4">
            <div class="d-inline-flex align-items-center justify-content-center mb-3" style="width: 64px; height: 64px; background: rgba(var(--role-color-rgb), 0.1); border-radius: 50%;">
                <i class="bi bi-person-lock fs-2" style="color: var(--role-color);"></i>
            </div>
            <h4 class="fw-bold mb-1">Instructor Recovery</h4>
            <p class="text-muted small">Enter your email and we'll help you reset your mentor account password.</p>
        </div>

        <div id="recoveryForm">
            @await Html.PartialAsync("_ForgotPasswordPartial", "")

            <div class="text-center mt-4 pt-3 border-top">
                <a asp-action="InstructorLogin" class="text-decoration-none small fw-bold text-muted">
                    Back to Login
                </a>
            </div>
        </div>
    </div>
</section>
