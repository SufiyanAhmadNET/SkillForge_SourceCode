<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>SkillForge – Instructor Dashboard</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.css" rel="stylesheet">
    <link href="/css/Instructor.css" rel="stylesheet" asp-append-version="true">
    <link href="~/css/alert.css" rel="stylesheet" asp-append-version="true"/>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</head>
<body>

    <div class="d-flex">

        <!-- ===== SIDEBAR ===== -->
        <aside class="sidebar d-flex flex-column">

            <div class="sidebar-brand px-4 py-3 d-flex align-items-center border-bottom border-secondary">
                <i class="bi bi-mortarboard-fill fs-5 me-1" style="color:var(--orange);"></i>
                <span class="fw-bold text-white">SkillForge</span>
                <span class="badge inst-badge ms-1">INSTRUCTOR</span>
            </div>

            <nav class="flex-grow-1 px-3 py-3">

                <p class="nav-section-label">OVERVIEW</p>
                <ul class="nav flex-column gap-1">
                    <li>
                        <a asp-area="Instructor" asp-controller="Home" asp-action="Dashboard" class="nav-link sidebar-link">
                            <i class="bi bi-grid me-2"></i>Dashboard
                        </a>
                    </li>
                </ul>

                @if (ViewBag.ApplicationStatus == SkillForge.Services.Instructors.Models.MentorApplicationStatus.Approved)
                {
                    <p class="nav-section-label mt-3">MY COURSES</p>
                    <ul class="nav flex-column gap-1">
                        <li>
                            <a asp-area="Instructor" asp-controller="Home" asp-action="MyCourses" class="nav-link sidebar-link">
                                <i class="bi bi-journal-bookmark me-2"></i>My Courses
                            </a>
                        </li>

                        <li>
                            <a asp-area="Instructor" asp-controller="Home" asp-action="AddCourse" class="nav-link sidebar-link">
                                <i class="bi bi-plus-circle me-2"></i>Add Course
                            </a>
                        </li>
                    </ul>

                    <p class="nav-section-label mt-3">FINANCE</p>
                    <ul class="nav flex-column gap-1">
                        <li>
                            <a asp-area="Instructor" asp-controller="Home" asp-action="Earning" class="nav-link sidebar-link">
                                <i class="bi bi-cash-stack me-2"></i>Earnings
                            </a>
                        </li>
                    </ul>
                }

                <p class="nav-section-label mt-3">ACCOUNT</p>
                <ul class="nav flex-column gap-1">
                    <li>
                        <a asp-area="Instructor" asp-controller="Home" asp-action="Profile" class="nav-link sidebar-link">
                        <i class="bi bi-person me-2"></i>Profile
                        </a>
                        </li>
                </ul>

            </nav>

            <div class="px-3 pb-3">
                <a href="javascript:void(0)" onclick="confirmLogoutInstructor('@Url.Action("Logout", "Auth", new { area = "Instructor" })')" class="nav-link sidebar-link text-danger">
                    <i class="bi bi-box-arrow-left me-2"></i>Logout
                </a>
            </div>

        </aside>
        <!-- ===== /SIDEBAR ===== -->


        <div class="flex-grow-1 d-flex flex-column main-wrap">

            <!-- HEADER -->
            <header class="inst-header d-flex align-items-center justify-content-between px-4 bg-white border-bottom">
                <span class="fw-semibold"></span>
                <div class="d-flex align-items-center gap-3">
                    <button class="btn btn-light btn-sm position-relative">
                        <i class="bi bi-bell"></i>
                        <span class="position-absolute top-0 start-100 translate-middle badge rounded-pill bg-danger" style="font-size:9px;">2</span>
                    </button>
                    <div class="d-flex align-items-center gap-2">
                        @if (!string.IsNullOrEmpty(ViewBag.PhotoPath) && ViewBag.PhotoPath != "/images/DefaultProfilePhoto.jfif")
                        {
                            <img src="@ViewBag.PhotoPath" class="rounded-circle" style="width:32px;height:32px;object-fit:cover;" alt="Avatar" />
                        }
                        else
                        {
                            <div class="inst-avatar">@(string.IsNullOrEmpty(ViewBag.FirstName) ? "G" : ViewBag.FirstName[0].ToString().ToUpper())</div>
                        }
                        <div>
                            <div class="fw-semibold" style="font-size:13px;">@(string.IsNullOrEmpty(ViewBag.FirstName) ? "Guest User" : $"{ViewBag.FirstName} {ViewBag.LastName}")</div>
                            <div class="text-muted" style="font-size:11px;">Instructor</div>
                        </div>
                    </div>
                </div>
            </header>
            <!-- /HEADER -->


            <partial name="_Alert_Message_Partial" />
            <partial name="_ConfirmModal" />

            <div class="inst-content-padding flex-grow-1">
                @RenderBody()
            </div>
            @RenderSection("Scripts", required: false)

            <script>
                function confirmLogoutInstructor(logoutUrl) {
                    SkillForge.confirm({
                        message: "Are you sure you want to log out from the instructor panel?",
                        confirmText: "Logout",
                        onConfirm: function() {
                            window.location.href = logoutUrl;
                        }
                    });
                }
            </script>
            <script src="~/js/Alert.js" asp-append-version="true"></script>
</body>
</html>
